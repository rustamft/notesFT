<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NotesFT" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/corn</item>
        <item name="colorPrimaryVariant">@color/new_bridge</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/corn</item>
        <item name="colorSecondaryVariant">@color/new_bridge</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?android:attr/colorBackground</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Button theme. -->
    <style name="Button" parent="Theme.NotesFT">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="textAllCaps">true</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- android:textColor="?colorOnSecondary" -->
    </style>

    <!-- FAB theme. -->
    <style name="FAB" parent="Button">
        <item name="android:layout_margin">16dp</item>
    </style>

    <!-- EditText theme. -->
    <style name="EditText" parent="Theme.NotesFT">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">top</item>
        <item name="android:textSize">18sp</item>
        <item name="android:layout_margin">12dp</item>
        <item name="android:inputType">textMultiLine</item>
        <item name="colorControlNormal">@color/corn</item>
        <item name="colorControlActivated">@color/corn</item>
        <item name="colorControlHighlight">@color/corn</item>
        <item name="android:textColorHighlight">@color/golden_oak</item>
    </style>

    <!-- DialogEditText theme. -->
    <style name="DialogEditText" parent="EditText">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:inputType">textShortMessage</item>
        <item name="backgroundTint">@color/corn</item>
    </style>
</resources>